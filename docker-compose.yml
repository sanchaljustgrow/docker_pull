version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testenvq-frontend
    ports:
      - "8080:80"
    env_file:
      - .env   # loads your .env file and makes it available to the container
    restart: always
