version: "3.8"

services:
  angular-frontend:
    image: sanchaljustgrow/new-test-frontend:latest
    container_name: angular-frontend
    environment:
       ENV NG_APP_URL: ${ENV NG_APP_URL}
    ports:
      - "8080:80"
