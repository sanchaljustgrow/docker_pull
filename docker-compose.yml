version: "3.8"

services:
  angular-frontend:
    build:
      context: https://github.com/your-org/frontend-repo.git
      dockerfile: Dockerfile
      args:
        NG_APP_URL: ${NG_APP_URL}
    container_name: angular-frontend
    ports:
      - "8081:80"
