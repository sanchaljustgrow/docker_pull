services:
  angular-frontend:
    image: sanchaljustgrow/angular-dockertest-frontend:latest
    container_name: angular-frontend
    ports:
      - "${APP_PORT:-8080}:80"
    env_file:
      - .env
    restart: always
