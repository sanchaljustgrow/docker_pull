version: "3.8"

services:
  angular-frontend:
    image: sanchaljustgrow/new-test-frontend:lates
    container_name: angular-frontend
    environment:
      - ENV NG_APP_URL="https://dummyjson.com/products/1"
    ports:
      - "8080:80"
